[project]
name = "robinhood-crypto-research-agent"
version = "0.1.0"
description = "Safe Robinhood Crypto API research and paper-trading scaffold."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
crypto-signing = ["PyNaCl>=1.5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
